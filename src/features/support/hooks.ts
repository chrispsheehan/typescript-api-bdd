import { Before } from '@cucumber/cucumber';
import { RichestApiAuth } from '../../richest-api/api-auth';

Before( function () {

    console.log('bbbbbbb');
    //this.token = (new RichestApiAuth()).getToken();
});